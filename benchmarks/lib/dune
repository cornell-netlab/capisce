(library
  (name pbench)
  (libraries core core_unix bignum ANSITerminal petr4 poulet4 hashcons)
  (preprocess (pps ppx_jane ppx_inline_test ppx_deriving_yojson ppx_deriving.std)))

(menhir
  (modules parser)
  (flags --explain))
(ocamllex lexer)
